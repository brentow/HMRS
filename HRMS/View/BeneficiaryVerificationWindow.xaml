<UserControl x:Class="HRMS.View.BeneficiaryVerificationWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1200"
             Background="White">
    <UserControl.Resources>
        <Color x:Key="PrimaryColor">#1E4368</Color>
        <Color x:Key="AccentColor">#FDBD55</Color>
        <Color x:Key="SuccessColor">#2E9D5B</Color>
        <Color x:Key="WarningColor">#D9B53B</Color>
        <Color x:Key="ErrorColor">#D84343</Color>
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E5EDF7" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Height" Value="36" />
            <Setter Property="MinWidth" Value="100" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ApproveButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}" />
        </Style>

        <Style x:Key="RejectButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ButtonStyle}">
            <Setter Property="Background" Value="#F0F5FD" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
        </Style>

        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F2F7FD" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,0,8,0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="GridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,4,8,4" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>

        <Style x:Key="GridRowStyle" TargetType="DataGridRow">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#1E4368" />
            <Setter Property="Background" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8F1FC" />
                    <Setter Property="Foreground" Value="#1E4368" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36" />
            <Setter Property="BorderBrush" Value="#D6E1F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#F7FAFF" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="36" />
            <Setter Property="BorderBrush" Value="#D6E1F0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="#F7FAFF" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Background" Value="#F2F7FD" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="8" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Beneficiary Verification" FontSize="22" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,12" />

        <!-- Status Summary -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Pending Count -->
                <Border Grid.Column="0" Style="{StaticResource StatusBadgeStyle}" Margin="0,0,12,0">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Pending" FontSize="12" Foreground="#666" />
                        <TextBlock Text="{Binding PendingCount}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource WarningBrush}" />
                    </StackPanel>
                </Border>

                <!-- Approved Count -->
                <Border Grid.Column="1" Style="{StaticResource StatusBadgeStyle}" Margin="0,0,12,0">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Approved" FontSize="12" Foreground="#666" />
                        <TextBlock Text="{Binding ApprovedCount}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" />
                    </StackPanel>
                </Border>

                <!-- Rejected Count -->
                <Border Grid.Column="2" Style="{StaticResource StatusBadgeStyle}">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Rejected" FontSize="12" Foreground="#666" />
                        <TextBlock Text="{Binding RejectedCount}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource ErrorBrush}" />
                    </StackPanel>
                </Border>

                <!-- Refresh Button -->
                <Button Grid.Column="4" Content="Refresh" Command="{Binding RefreshCommand}" 
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </Grid>
        </Border>

        <!-- Filters and Controls -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Filter -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Status Filter:" Foreground="{StaticResource PrimaryBrush}" 
                               FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <ComboBox Grid.Column="1" ItemsSource="{Binding StatusFilters}" SelectedItem="{Binding SelectedStatusFilter}"
                              Style="{StaticResource ComboBoxStyle}" />
                </Grid>

                <!-- Beneficiary DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding Beneficiaries}" SelectedItem="{Binding SelectedBeneficiary}"
                          AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                          ColumnHeaderStyle="{StaticResource HeaderStyle}" CellStyle="{StaticResource GridCellStyle}"
                          RowStyle="{StaticResource GridRowStyle}" RowHeight="36">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Civil Registry ID" Binding="{Binding CivilRegistryID}" Width="140" />
                        <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120" />
                        <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120" />
                        <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*" />
                        <DataGridTextColumn Header="Imported" Binding="{Binding ImportedAt, StringFormat='MMM dd, yyyy'}" Width="110" />
                        <DataGridTextColumn Header="Status" Binding="{Binding VerificationStatus}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Details and Actions -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <Grid Visibility="{Binding SelectedBeneficiary, Converter={StaticResource ObjectToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Details -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Name:" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <TextBlock Text="{Binding SelectedBeneficiary.FirstName}" Foreground="#333" />
                        <TextBlock Text=" " Foreground="#333" />
                        <TextBlock Text="{Binding SelectedBeneficiary.LastName}" Foreground="#333" />
                    </StackPanel>

                    <TextBlock Grid.Column="2" Text="Civil Registry ID:" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" Margin="12,0,0,0" />
                    <TextBlock Grid.Column="3" Text="{Binding SelectedBeneficiary.CivilRegistryID}" Foreground="#333" Margin="8,0" />
                </Grid>

                <!-- Remarks Input -->
                <Grid Grid.Row="1" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Remarks (Optional):" Foreground="{StaticResource PrimaryBrush}" 
                               FontWeight="SemiBold" VerticalAlignment="Top" Margin="0,4" />
                    <TextBox Grid.Column="1" Text="{Binding RemarksInput, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource InputStyle}" Height="60" VerticalAlignment="Stretch"
                             AcceptsReturn="True" TextWrapping="Wrap" Margin="8,0,0,0" />
                </Grid>

                <!-- Action Buttons -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Content="Approve" Command="{Binding ApproveBeneficiaryCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                            Style="{StaticResource ApproveButtonStyle}" Margin="0,0,8,0" />
                    <Button Grid.Column="1" Content="Reject" Command="{Binding RejectBeneficiaryCommand}"
                            IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                            Style="{StaticResource RejectButtonStyle}" />
                </Grid>
            </Grid>
        </Border>

        <!-- Status Message -->
        <Border Grid.Row="4" Style="{StaticResource CardStyle}" Background="{Binding ActionMessageBrush}">
            <TextBlock Text="{Binding ActionMessage}" Foreground="White" FontWeight="SemiBold" />
        </Border>
    </Grid>
</UserControl>
