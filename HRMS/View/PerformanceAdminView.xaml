<UserControl x:Class="HRMS.View.PerformanceAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             Background="White">

    <UserControl.Resources>
        <Color x:Key="PrimaryColor">#1E4368</Color>
        <Color x:Key="AccentColor">#FDBD55</Color>

        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E5EDF7" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="12" />
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.93" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.85" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserResizeRows" Value="False" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="RowHeaderWidth" Value="0" />
            <Setter Property="RowHeight" Value="44" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#F8FBFF" />
            <Setter Property="HorizontalGridLinesBrush" Value="#E9F0FA" />
            <Setter Property="VerticalGridLinesBrush" Value="#E9F0FA" />
            <Setter Property="ColumnHeaderHeight" Value="42" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="SelectionUnit" Value="FullRow" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ColumnHeaderStyle" Value="{DynamicResource HeaderStyle}" />
            <Setter Property="RowStyle" Value="{DynamicResource RowStyle}" />
            <Setter Property="CellStyle" Value="{DynamicResource CellStyle}" />
        </Style>

        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F2F7FD" />
            <Setter Property="Foreground" Value="#1E4368" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderBrush" Value="#E3ECF8" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="8,0,8,0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="CenteredHeaderStyle" TargetType="DataGridColumnHeader" BasedOn="{StaticResource HeaderStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="RowStyle" TargetType="DataGridRow">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#1E4368" />
            <Setter Property="Background" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8F1FC" />
                    <Setter Property="Foreground" Value="#1E4368" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,0,8,0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>

        <Style x:Key="CenterCellStyle" TargetType="DataGridCell" BasedOn="{StaticResource CellStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="CenterCellTextStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="InlineStatusComboStyle" TargetType="ComboBox" BasedOn="{x:Null}">
            <Setter Property="Height" Value="28" />
            <Setter Property="MinWidth" Value="72" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="TextBlock.TextAlignment" Value="Center" />
            <Setter Property="Padding" Value="6,0,6,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#C8D8EC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LeftCellTextStyle" TargetType="TextBlock">
            <Setter Property="TextAlignment" Value="Left" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0" />
        </Style>

        <Style x:Key="TrimmedCellTextStyle" TargetType="TextBlock" BasedOn="{StaticResource LeftCellTextStyle}">
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="CenterEditTextBoxStyle" TargetType="TextBox">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="InlineDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Height" Value="30" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="Performance Admin Workspace"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}" />
                <TextBlock Text="Manage cycles and reviews in one place."
                           Foreground="#5B6A7A"
                           Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Refresh"
                        Width="100"
                        Margin="0,0,8,0"
                        Style="{StaticResource ActionButtonStyle}"
                        Click="Refresh_Click" />
                <Button Content="Close"
                        Width="100"
                        Style="{StaticResource ActionButtonStyle}"
                        Background="{StaticResource AccentBrush}"
                        Foreground="{StaticResource PrimaryBrush}"
                        Click="Close_Click" />
            </StackPanel>
        </Grid>

        <TabControl Grid.Row="1" Background="White" BorderThickness="0">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <materialDesign:PackIcon Kind="RepeatVariant" Width="18" Height="18" Margin="0,0,6,0" />
                        <TextBlock Text="Performance Cycles" FontWeight="SemiBold" />
                    </StackPanel>
                </TabItem.Header>
                <Border Style="{StaticResource CardStyle}" Margin="0,10,0,0">
                    <DataGrid Style="{StaticResource GridStyle}"
                              IsReadOnly="False"
                              ItemsSource="{Binding Cycles}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code"
                                                Width="0.9*"
                                                Binding="{Binding CycleCode}"
                                                HeaderStyle="{StaticResource HeaderStyle}"
                                                ElementStyle="{StaticResource LeftCellTextStyle}"
                                                IsReadOnly="True" />
                            <DataGridTextColumn Header="Name"
                                                Width="2.2*"
                                                Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                HeaderStyle="{StaticResource HeaderStyle}"
                                                ElementStyle="{StaticResource TrimmedCellTextStyle}"
                                                EditingElementStyle="{StaticResource CenterEditTextBoxStyle}" />
                            <DataGridTemplateColumn Header="Start" Width="1.15*" HeaderStyle="{StaticResource CenteredHeaderStyle}" CellStyle="{StaticResource CenterCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding StartDate, StringFormat={}{0:MMM dd, yyyy}}"
                                                   Style="{StaticResource CenterCellTextStyle}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <DatePicker Style="{StaticResource InlineDatePickerStyle}"
                                                    SelectedDate="{Binding StartDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="End" Width="1.15*" HeaderStyle="{StaticResource CenteredHeaderStyle}" CellStyle="{StaticResource CenterCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding EndDate, StringFormat={}{0:MMM dd, yyyy}}"
                                                   Style="{StaticResource CenterCellTextStyle}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <DatePicker Style="{StaticResource InlineDatePickerStyle}"
                                                    SelectedDate="{Binding EndDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Status" Width="1.05*" HeaderStyle="{StaticResource CenteredHeaderStyle}" CellStyle="{StaticResource CenterCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox Style="{StaticResource InlineStatusComboStyle}"
                                                  SelectedValue="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  SelectedValuePath="Content">
                                            <ComboBoxItem Content="DRAFT" />
                                            <ComboBoxItem Content="OPEN" />
                                            <ComboBoxItem Content="CLOSED" />
                                            <ComboBoxItem Content="ARCHIVED" />
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Created By"
                                                Width="1.45*"
                                                Binding="{Binding CreatedBy}"
                                                HeaderStyle="{StaticResource HeaderStyle}"
                                                ElementStyle="{StaticResource TrimmedCellTextStyle}"
                                                IsReadOnly="True" />
                            <DataGridTemplateColumn Header="Action" Width="0.8*" HeaderStyle="{StaticResource CenteredHeaderStyle}" CellStyle="{StaticResource CenterCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Save"
                                                Width="58"
                                                Style="{StaticResource ActionButtonStyle}"
                                                Margin="0"
                                                Click="SaveCycle_Click" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <materialDesign:PackIcon Kind="ClipboardTextOutline" Width="18" Height="18" Margin="0,0,6,0" />
                        <TextBlock Text="Performance Reviews" FontWeight="SemiBold" />
                    </StackPanel>
                </TabItem.Header>
                <Border Style="{StaticResource CardStyle}" Margin="0,10,0,0">
                    <DataGrid Style="{StaticResource GridStyle}"
                              IsReadOnly="False"
                              ItemsSource="{Binding Reviews}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Cycle"
                                                Width="0.9*"
                                                Binding="{Binding CycleCode}"
                                                HeaderStyle="{StaticResource HeaderStyle}"
                                                ElementStyle="{StaticResource LeftCellTextStyle}"
                                                IsReadOnly="True" />
                            <DataGridTextColumn Header="Employee"
                                                Width="1.6*"
                                                Binding="{Binding Employee}"
                                                HeaderStyle="{StaticResource HeaderStyle}"
                                                ElementStyle="{StaticResource TrimmedCellTextStyle}"
                                                IsReadOnly="True" />
                            <DataGridTextColumn Header="Reviewer"
                                                Width="1.5*"
                                                Binding="{Binding Reviewer}"
                                                HeaderStyle="{StaticResource HeaderStyle}"
                                                ElementStyle="{StaticResource TrimmedCellTextStyle}"
                                                IsReadOnly="True" />
                            <DataGridTextColumn Header="Rating"
                                                Width="0.7*"
                                                Binding="{Binding Rating, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                                HeaderStyle="{StaticResource CenteredHeaderStyle}"
                                                ElementStyle="{StaticResource CenterCellTextStyle}"
                                                EditingElementStyle="{StaticResource CenterEditTextBoxStyle}" />
                            <DataGridTemplateColumn Header="Status" Width="1.0*" HeaderStyle="{StaticResource CenteredHeaderStyle}" CellStyle="{StaticResource CenterCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox Style="{StaticResource InlineStatusComboStyle}"
                                                  SelectedValue="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  SelectedValuePath="Content">
                                            <ComboBoxItem Content="DRAFT" />
                                            <ComboBoxItem Content="SUBMITTED" />
                                            <ComboBoxItem Content="APPROVED" />
                                            <ComboBoxItem Content="REJECTED" />
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Items"
                                                Width="0.6*"
                                                Binding="{Binding ItemsCount}"
                                                IsReadOnly="True"
                                                CellStyle="{StaticResource CenterCellStyle}"
                                                HeaderStyle="{StaticResource CenteredHeaderStyle}"
                                                ElementStyle="{StaticResource CenterCellTextStyle}" />
                            <DataGridTextColumn Header="Remarks"
                                                Width="1.9*"
                                                Binding="{Binding Remarks, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                HeaderStyle="{StaticResource HeaderStyle}"
                                                ElementStyle="{StaticResource TrimmedCellTextStyle}"
                                                EditingElementStyle="{StaticResource CenterEditTextBoxStyle}" />
                            <DataGridTemplateColumn Header="Action" Width="0.8*" HeaderStyle="{StaticResource CenteredHeaderStyle}" CellStyle="{StaticResource CenterCellStyle}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Save"
                                                Width="58"
                                                Style="{StaticResource ActionButtonStyle}"
                                                Margin="0"
                                                Click="SaveReview_Click" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
