<Window x:Class="HRMS.View.EmployeeAttendanceLogsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Employee Attendance Logs"
        Width="1220"
        Height="760"
        MinWidth="1040"
        MinHeight="640"
        WindowStartupLocation="CenterOwner"
        WindowState="Maximized"
        Background="#F5F8FC">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1E4368" />
        <SolidColorBrush x:Key="LineBrush" Color="#DCE7F3" />
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="MinWidth" Value="130" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SoftButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#EAF2FB" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#D5E3F5" />
        </Style>
        <Style x:Key="GridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F2F7FD" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderBrush" Value="#E3ECF8" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="Padding" Value="8,0,8,0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="GridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,0,8,0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="TextBlock.TextAlignment" Value="Center" />
            <Setter Property="TextBlock.VerticalAlignment" Value="Center" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
        <Style x:Key="GridRowStyle" TargetType="DataGridRow">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#1E4368" />
            <Setter Property="Background" Value="White" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E8F1FC" />
                    <Setter Property="Foreground" Value="#1E4368" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="ClipboardTextClockOutline" Width="30" Height="28" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,10,0" />
                    <TextBlock x:Name="HeaderTextBlock" Text="Employee Attendance Logs" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
                </StackPanel>
                <TextBlock x:Name="SubHeaderTextBlock" Margin="0,4,0,0" Foreground="#5B6A7A" Text="Morning in, morning out, afternoon in, and afternoon out." />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource SoftButtonStyle}" Margin="0,0,8,0" Click="BackButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ArrowLeft" Width="16" Height="16" Margin="0,0,8,0" />
                        <TextBlock Text="Back" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SoftButtonStyle}" Margin="0,0,8,0" Click="PrintButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PrinterOutline" Width="16" Height="16" Margin="0,0,8,0" />
                        <TextBlock Text="Print" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0" Click="ViewPdfButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FilePdfBox" Width="16" Height="16" Margin="0,0,8,0" />
                        <TextBlock Text="View PDF" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource PrimaryButtonStyle}" Click="ExportButton_OnClick">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FileExportOutline" Width="16" Height="16" Margin="0,0,8,0" />
                        <TextBlock Text="Export" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1"
                Margin="0,0,0,14"
                Padding="12,10"
                BorderBrush="{StaticResource LineBrush}"
                BorderThickness="1"
                Background="White"
                CornerRadius="10">
            <TextBlock x:Name="SummaryTextBlock"
                       Foreground="#4A5B6C"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap" />
        </Border>

        <Border Grid.Row="2"
                Padding="14"
                CornerRadius="14"
                BorderBrush="{StaticResource LineBrush}"
                BorderThickness="1"
                Background="White">
            <DataGrid x:Name="LogsGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="1"
                      BorderBrush="#E3ECF8"
                      RowHeaderWidth="0"
                      RowBackground="White"
                      AlternatingRowBackground="#F8FBFF"
                      HorizontalGridLinesBrush="#E9F0FA"
                      VerticalGridLinesBrush="#E9F0FA"
                      ColumnHeaderHeight="40"
                      RowHeight="44"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      SelectionMode="Single"
                      SelectionUnit="FullRow">
                <DataGrid.ColumnHeaderStyle>
                    <Style BasedOn="{StaticResource GridHeaderStyle}" TargetType="DataGridColumnHeader" />
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style BasedOn="{StaticResource GridCellStyle}" TargetType="DataGridCell" />
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style BasedOn="{StaticResource GridRowStyle}" TargetType="DataGridRow" />
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding WorkDateText}" Width="1.1*" />
                    <DataGridTextColumn Header="Morning In" Binding="{Binding MorningIn}" Width="1*" />
                    <DataGridTextColumn Header="Morning Out" Binding="{Binding MorningOut}" Width="1*" />
                    <DataGridTextColumn Header="Afternoon In" Binding="{Binding AfternoonIn}" Width="1*" />
                    <DataGridTextColumn Header="Afternoon Out" Binding="{Binding AfternoonOut}" Width="1*" />
                    <DataGridTextColumn Header="Punches" Binding="{Binding TotalPunches}" Width="0.8*" />
                    <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="1.8*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Window>
