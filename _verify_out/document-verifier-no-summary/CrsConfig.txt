# CRS Database Connection Settings Template
# Configure remote CRS values through the CRS_DB_HOST, CRS_DB_PORT,
# CRS_DB_NAME, CRS_DB_USER, and CRS_DB_PASSWORD user environment variables.
# Settings saved inside the application use ProtectedPassword instead of plaintext.
