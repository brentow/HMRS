<UserControl x:Class="HRMS.View.PerformanceWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:HRMS.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             Background="White">
    <UserControl.Resources>
        <Color x:Key="PrimaryColor">#1E4368</Color>
        <Color x:Key="LightColor">#CBE9FE</Color>
        <Color x:Key="AccentColor">#FDBD55</Color>
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}" />
        <SolidColorBrush x:Key="LightBrush" Color="{StaticResource LightColor}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />

        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E5EDF7" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Margin" Value="6" />
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.DataContext>
            <vm:PerformanceViewModel/>
        </Grid.DataContext>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- Header -->
                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Performance" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
                        <TextBlock Text="Cycles, reviews, and ratings" Foreground="#5B6A7A" />
                    </StackPanel>
                </Grid>

                <!-- Stat cards -->
                <UniformGrid Columns="4" Rows="1" Margin="0,0,0,16">
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Total Cycles" Foreground="#6A7684"/>
                            <TextBlock Text="{Binding TotalCycles}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Open Cycles" Foreground="#6A7684"/>
                            <TextBlock Text="{Binding OpenCycles}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Reviews" Foreground="#6A7684"/>
                            <TextBlock Text="{Binding TotalReviews}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Avg Rating" Foreground="#6A7684"/>
                            <TextBlock Text="{Binding AvgRating, StringFormat={}{0:F1}}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Review progress -->
                    <Border Grid.Column="0" Style="{StaticResource StatCardStyle}" Padding="18" Margin="6,6,6,6">
                        <StackPanel>
                            <TextBlock Text="Review Progress" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>
                            <ItemsControl ItemsSource="{Binding ReviewChart}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" Foreground="#4A5664"/>
                                                <TextBlock Text="{Binding Value}" Grid.Column="1" Foreground="#4A5664"/>
                                            </Grid>
                                            <ProgressBar Height="12"
                                                         Value="{Binding Value}"
                                                         Maximum="{Binding DataContext.TotalReviews, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                         Background="#E6F3FF">
                                                <ProgressBar.Foreground>
                                                    <SolidColorBrush Color="{Binding ColorHex}" />
                                                </ProgressBar.Foreground>
                                            </ProgressBar>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Top performers -->
                    <Border Grid.Column="1" Style="{StaticResource StatCardStyle}" Padding="18" Margin="6,6,6,6">
                        <StackPanel>
                            <TextBlock Text="Top Performers" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10"/>
                            <ItemsControl ItemsSource="{Binding TopPerformers}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#1E4368"/>
                                            <Border Grid.Column="1" Background="#CBE9FE" CornerRadius="8" Padding="10,4" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Rating, StringFormat={}{0:F1}}" Foreground="#1E4368" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
